Instructions for running the suite:

1.
2.
3. nosetests -vs tests/

General notes:
Folder struct would be more complex, separate data from tests (no hard-coded data)

Chromedriver binary must be in PATH